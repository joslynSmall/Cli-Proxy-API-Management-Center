# 技术文档（自动同步区块）

## 与代码同步（自动维护）
<!-- gdocs:auto:technical:start -->
### Index

#### Added/Changed
- 涉及代码变更（未提取到具体事实点） (evidence: staged diff)
- 新增依赖 `@testing-library/jest-dom`（^6.9.1） (evidence: package.json)
- 新增依赖 `@testing-library/react`（^16.3.0） (evidence: package.json)
- 新增依赖 `@testing-library/user-event`（^14.6.1） (evidence: package.json)
- 新增依赖 `jsdom`（^26.1.0） (evidence: package.json)
- 新增依赖 `vitest`（^3.2.4） (evidence: package.json)

#### Removed/Deprecated
<!-- gdocs:auto:technical:end -->
